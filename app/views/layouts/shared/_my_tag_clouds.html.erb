<% user = @other_user.nil? ? current_user : @other_user %>
<div class='title'>
  My tag clouds 
</div>

<div id='tag_cloud'>
  <% tag_cloud user.owned_my_tag_counts, %w{s m l} do |tag, css_class| %>
    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
  <% end %>
</div>
